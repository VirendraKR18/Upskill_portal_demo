# User Story - US_001

## Quick Reference Schema
```yaml
# User Story:
  * ID: [US_001]
  * Title: [SSO Authentication via Corporate Identity Provider]
## Description:
   * [As a platform user, I want to authenticate using my corporate SSO credentials (OAuth 2.0/SAML 2.0), so that I can securely access the learning platform without managing separate credentials.]
## Acceptance Criteria:
   * [See detailed section below]
## Edge Cases:
   * [See detailed section below]
## Traceability:
### Parent:
    * [EP-001]
### Tags:
    * [FR-001, NFR-004, NFR-005, UC-007]
### Dependencies:
    * [N/A - First EP-001 story]
```
---

## Story ID
   * ID Format: US_001

## Story Title
   * SSO Authentication via Corporate Identity Provider

## Description
  * As a platform user, I want to authenticate using my corporate SSO credentials (OAuth 2.0/SAML 2.0), so that I can securely access the learning platform without managing separate credentials.

## Acceptance Criteria
  1. **Given** I navigate to the platform login page, **When** I click "Sign In", **Then** I am redirected to the Organization SSO identity provider login page.

  2. **Given** I enter valid corporate credentials on the SSO page, **When** the identity provider authenticates me, **Then** the system receives an auth token with claims (roles, email, employee ID), creates a JWT session token, and redirects me to the Individual Dashboard.

  3. **Given** I am authenticated, **When** the system processes my SSO claims, **Then** my role (Learner, Manager, Admin, or Leadership) is mapped from SSO claims and stored in the JWT for subsequent authorization checks.

  4. **Given** I enter invalid credentials, **When** authentication fails at the SSO provider, **Then** I remain on the SSO login page with an error message, and the platform logs the failed authentication attempt (timestamp, IP address, employee ID if available).

  5. **Given** the SSO provider is temporarily unavailable, **When** I attempt to log in, **Then** the system displays a user-friendly error message ("Authentication service temporarily unavailable. Please try again in a few minutes.") and does not expose internal error details.

  6. **Given** data is transmitted between the platform and SSO provider, **When** any authentication request is made, **Then** all communication uses TLS 1.2+ encryption in transit.

## Edge Cases
   * What happens when the SSO token is tampered with? (JWT signature validation rejects tampered tokens; request blocked and logged as security event)
   * How does the system handle SSO token with unrecognized role claims? (Default to Learner role; log warning for admin review; do not block access)
   * What happens if the user's EmployeeID from SSO claims does not exist in the User table? (Block access; return "Account not provisioned" message; user must wait for next Workday sync cycle)

## Traceability
### Parent Epic
    * Epic: EP-001 (Authentication & User Management)

### Requirement Tags
    * FR-001 (SSO Authentication)
    * NFR-004 (SSO authentication OAuth 2.0/SAML 2.0)
    * NFR-005 (Data encryption at rest and in transit)
    * UC-007 (System Syncs Employee Data - SSO claims mapping)

### Dependencies
    * N/A - This is the first EP-001 story (requires EP-TECH infrastructure and EP-DATA User entity as epic-level dependencies)

## Visual Design Context (UI Stories Only)
### Screen Specifications
- **Screen ID(s)**: SCR-001 (Login)
- **Figma Spec Reference**: .propel/context/docs/figma_spec.md#SCR-001

### Wireframe References
| Field | Value |
|-------|-------|
| **Status** | AVAILABLE |
| **Type** | HTML |
| **Path/URL** | .propel/context/wireframes/Hi-Fi/wireframe-SCR-001-login.html |

### UX Requirements
- **UXR Mappings**: UXR-001 (Max 3 clicks), UXR-101-105 (Accessibility), UXR-201-204 (Responsiveness), UXR-301-303 (Visual Design), UXR-401-404 (Interaction), UXR-501-504 (Error Handling)
